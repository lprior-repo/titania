v1.5 cargo-kani Harness Inventory Summary
==========================================

Tool: cargo-kani 0.67.0
Generated at: 2026-07-16T02:54:04Z

Command issue: cargo-kani list does NOT accept -p / --package.
The subcommand signature is "cargo-kani list [OPTIONS]" with no package
selector. The literal command `cargo kani list --format json -p <pkg>`
is rejected for every package with:
    error: unexpected argument '-p' found

Fallback that produced real inventory: chdir into the crate and run
`cargo kani list --format json`. cargo-kani 0.67.0 writes the discovered
harness inventory to <crate>/kani-list.json (not to stdout). The stdout
from that fallback is the banner + warnings + "Wrote list results to ..."
line; the JSON contents were copied from <crate>/kani-list.json into
.evidence/v1.5/raw/kani-list-<pkg>.json for each package.

Per-package harness counts
--------------------------
titania-core       :  8 harnesses
titania-lanes      :  0 harnesses
titania-check      :  0 harnesses
titania-aggregate  :  0 harnesses
titania-policy     :  0 harnesses
titania-output     :  0 harnesses
                 -----------
TOTAL              :  8 harnesses

titania-core harness inventory
------------------------------
File: crates/titania-core/src/kani.rs
  - kani::lane_digest_accepts_passed_not_greater_than_scanned
  - kani::lane_digest_rejects_passed_greater_than_scanned
  - kani::lane_name_rejects_empty_string
  - kani::lane_name_rejects_nul_byte
  - kani::recorded_target_root_accepts_absolute_path
  - kani::recorded_target_root_rejects_empty_string
  - kani::recorded_target_root_rejects_nul_byte
  - kani::recorded_target_root_rejects_relative_path

The kani-list JSON for titania-core also reported:
    "kani-version": "0.67.0"
    "file-version": "0.1"
    "totals": { "standard-harnesses": 8, "contract-harnesses": 0, "functions-under-contract": 0 }
A non-fatal warning was emitted for titania-core only:
    warning: Found the following unsupported constructs:
             - caller_location (1)
             - foreign function (2)
    Verification will fail if one or more of these constructs is reachable.

titania-lanes, titania-check, titania-aggregate, titania-policy,
titania-output
----------------------------------------------------------------
Every kani-list JSON for these five packages reports zero standard or
contract harnesses, e.g.:
    "kani-version": "0.67.0"
    "file-version": "0.1"
    "standard-harnesses": {}
    "contract-harnesses": {}
    "totals": { "standard-harnesses": 0, "contract-harnesses": 0, "functions-under-contract": 0 }

Failure evidence
----------------
The literal command `cargo kani list --format json -p <pkg>` failed for
ALL six packages. The captured stderr (saved to
.evidence/v1.5/raw/kani-list-<pkg>.literal.stderr) is identical for each:
    error: unexpected argument '-p' found
    Usage: cargo-kani list [OPTIONS]
    For more information, try '--help'.

After switching to the chdir-into-crate fallback, every invocation
exited 0. The kani-list JSON files are real cargo-kani output
(`kani-version` 0.67.0, `file-version` 0.1) parsed directly to populate
the combined kani-harnesses.json.

Artifacts
---------
.evidence/v1.5/kani-harnesses.json                          (combined inventory, schema v1)
.evidence/v1.5/raw/kani-list-summary.txt                    (this file)
.evidence/v1.5/raw/kani-list-titania-<pkg>.json             (raw kani JSON output, per package)
.evidence/v1.5/raw/kani-list-titania-<pkg>.stdout           (stdout from fallback, per package)
.evidence/v1.5/raw/kani-list-titania-<pkg>.stderr           (stderr from fallback, per package)
.evidence/v1.5/raw/kani-list-titania-<pkg>.literal.stderr   (literal -p failure, per package)
